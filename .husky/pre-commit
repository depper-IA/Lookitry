#!/usr/bin/env sh
# Husky pre-commit hook - runs frontend lint only

echo "Verificando codigo..."

cd frontend && pnpm run lint
FRONTEND_LINT=$?
cd ..

if [ $FRONTEND_LINT -ne 0 ]; then
  echo "Frontend lint fallo"
  exit 1
fi

echo "Lint pasado"
exit 0