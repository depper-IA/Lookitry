server {
    listen       80;
    server_name  localhost;

    error_page 404 /index.html;
    
    # Catch all requests and return the maintenance page with a 200/503 status
    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        try_files $uri /index.html;
    }
}
