# 20 de Abril 2026

## Recovery Completado

Todos los servicios recuperados en VPS 31.220.18.39:
- Backend, Frontend, n8n, Redis, MinIO, Traefik corriendo
- TLS/SSL funcionando
- SUPABASE_SERVICE_KEY corregido (era anon key, ahora service_role)
- Redis AUTH habilitado
- Puertos 8080, 5678, 9001 cerrados

## Problema img-proxy 403

Img-proxy retornando 403 para `wilkiedevs.com`.
Allowlist en pruebalo.controller.ts solo incluía dominios Lookitry.
Sam dijo que esto ya estaba arreglado en Cerebro - leer antes de actuar.

**Acción tomada**: Agregado `wilkiedevs.com` y `www.wilkiedevs.com` al allowlist en `pruebalo.controller.ts`.

## Fixes aplicados hoy (20 Abr 2026)

| Fix | Descripción |
|-----|-------------|
| img-proxy allowlist | Agregado `wilkiedevs.com` al allowlist de dominios permitidos |
| bypass_ip_protection | Cambiado de `true` a `false` en payment_settings |
| isTrialAbuse | ELIMINADO - código muerto que nunca se llamaba |

## IP Whitelist System (Ahora activo)

- `payment_settings.bypass_ip_protection` = **false** (protección activa)
- `payment_settings.ip_whitelist` = "" (vacío por ahora)
- El sistema ahora bloquea IPs con múltiples trials en los últimos 30 días
- Para agregar IPs que siempre pasen: actualizar `ip_whitelist` con IPs separadas por coma